{
  "name": "visionflow",
  "version": "1.0.0",
  "description": "Screen recording with AI processing",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc && cp src/renderer/index.html dist/renderer/ && cp src/renderer/styles.css dist/renderer/",
    "start": "npm run build && electron .",
    "dev": "npm run build && electron ."
  },
  "keywords": ["electron", "screen-recording", "video"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.10.0",
    "electron": "^28.0.0",
    "typescript": "^5.3.0"
  }
}
